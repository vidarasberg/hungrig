import { api } from "@/utils/api";
import {
  SignedIn,
  SignedOut,
  SignInButton,
  UserButton,
  useUser,
} from "@clerk/nextjs";
import { type NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  const { data } = api.recipes.getAll.useQuery();

  const { user } = useUser();

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b from-[#fafafe] to-[#d4d4d4]">
        <SignedIn>
          <UserButton />
          {user?.fullName}
          {data?.map((x) => (
            <a href={x.link} target="_blank" key={x.id}>
              {x.name}
            </a>
          ))}
        </SignedIn>
        <SignedOut>
          <SignInButton />
        </SignedOut>
      </main>
    </>
  );
};

export default Home;
